name: Terraform Infrastructure Pipeline

on:
  push:
    branches:
      - main

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Print Repo Structure
        run: ls -R

    defaults:
      run:
        working-directory: iac/terraform/envs/dev
